var dataStorage = dataStorage || (function(dataStorage){	/*Storage use jquery.cookie.js*/	var localStorageAvailable = false;	if(typeof(Storage)!=="undefined")	{		localStorageAvailable = true; 	}else{		$.cookie.raw = true;	}	dataStorage.set = function(valName, valValue){		if(localStorageAvailable){			localStorage[valName] = valValue;		} else {		    $.removeCookie(valName, { path: '/' });			$.cookie(valName, valValue, { expires: 365, path: '/' });		}	};	dataStorage.get = function(valName){		if(localStorageAvailable){			return localStorage[valName] || null;		}else{			return $.cookie(valName) || null;		}	 };		return dataStorage;}(dataStorage || {}))