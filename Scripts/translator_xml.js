var Translator = Translator || (function(Translator){	var xml = '<languages><hello><fr>Bonjour</fr><en>Hello</en></hello><title><fr>Titre</fr><en>Title</en></title></languages>';	var currentLanguage = 1;	var languages = ['fr', 'en'];	function xmlParser(){		var xmlDoc;		if (window.DOMParser){// code for IE7+, Firefox, Chrome, Opera, Safari			parser=new DOMParser();			xmlDoc=parser.parseFromString(xml,"text/xml");		}else{// code for IE6, IE5			xmlDoc=new ActiveXObject("Microsoft.XMLDOM");			xmlDoc.async=false;			xmlDoc.loadXML(xml); 		}			return xmlDoc;	}	Translator.translate = function (word){		var parser = xmlParser();		var text = parser.getElementsByTagName(word)[0];		var language = languages[currentLanguage];		return text.getElementsByTagName(language)[0].childNodes[0].nodeValue;	}		Translator.swithLanguage = function(langue){		currentLanguage = langue;	}		return Translator;}(Translator || {}))