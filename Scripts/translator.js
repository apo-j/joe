var translator = translator || (function(translator){	console.log('in translator');	if(!dataStorage.get(static.currentLanguage)){		console.log('reset langue');		dataStorage.set(static.currentLanguage, types.languages.Chinese);	}	var language = dataStorage.get(static.currentLanguage);		translator.translate = function (word){		if(!resources[word]){			return 'NOT_FOUND ( ' + word + ' ) ';		}		if(!resources[word][language]){			return 'NOT_FOUND_' + language + ' ( ' + word + ' ) ';		}		return resources[word][language];			};		translator.currentLanguage = function(){		return language;	};		translator.swithLanguage = function(langue){		if(language == langue){			return false;		}		language = langue;		dataStorage.set(static.currentLanguage, langue);		return true;	};		return translator;}(translator || {}, resources || {}))